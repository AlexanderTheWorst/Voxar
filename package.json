{
  "name": "voxar",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "install:all": "git pull && npm i && cd website && npm i && cd ../ && cd bot && npm i",
    "compose": "sudo docker compose -v && sudo docker compose up --build -d",
    "dev": "concurrently --raw \"npm run dev --workspace=@voxar/website\" \"npm run dev --workspace=@voxar/bot\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "workspaces": [
    "website",
    "bot",
    "mongodb"
  ],
  "dependencies": {
    "dotenv": "^17.2.1",
    "dotenv-cli": "^10.0.0"
  }
}
